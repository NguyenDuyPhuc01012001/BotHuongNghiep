import 'package:flutter/material.dart';

class QuestionSolutionScreen extends StatelessWidget {
  const QuestionSolutionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Question Solution Screen")),
    );
  }
}
